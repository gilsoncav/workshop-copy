org: jetpax # Enter your tenant name here
app: workshop # Enter your application name here
service: fullstack-website

provider:
  name: aws
  runtime: nodejs12.x
  region: sa-east-1

plugins:
  - serverless-finch

custom:
  client:
    bucketName: jpx-workshop-s3bucket # Change this to a universally unique bucket name
    distributionFolder: ./build

